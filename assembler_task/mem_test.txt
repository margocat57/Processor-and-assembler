CALL :1
:1
    CALL :2 ; input and find r^2
    CALL :3 ; find quotitent and reminder of div /10
    ; check if point in square and fill ram
    CALL :5 ; draw and finish program

;============================
; Input R and find R^2
; Input: r - rcx,
; Output: r^2      - rcx,
; centre of sq(Ox) - rax,
; centre of sq(Oy) - rbx,
; 0 point (0, 0)   - rex
;============================
:2
    PUSH 5
    POPR RAX
    PUSH 5
    POPR RBX
    IN
    POPR RCX
    PUSHR RCX
    PUSHR RCX
    MUL
    POPR RCX
    PUSH 0
    POPR REX
    RET
;============================
; Output circle
;============================
:5
    DRAW
    HLT
;============================
; Find points in the circle
; Input: r            - rcx,
; index of ram array  - rex
;
; Find coordinate of elem
; with each index of ram array
; and checks if it in square
;
; If it in square push 1 to
; ram elem(:6), in other case
; push 0
;
; JMP to :7 to check usage 
; of ram
;============================
:3
    PUSHR REX
    PUSH 10
    DIV
    POPR RFX
    PUSHR REX
    PUSHR RFX
    PUSH 10
    MUL
    SUB
    POPR RGX
    PUSHR RFX
    PUSHR RAX
    SUB
    PUSHR RFX
    PUSHR RAX
    SUB
    MUL
    PUSHR RGX
    PUSHR RBX
    SUB
    PUSHR RGX
    PUSHR RBX
    SUB
    MUL
    ADD
    PUSHR RCX
    JAE :6
    PUSH 0
    POPM [EX]
    JMP :7
:6
    PUSH 1
    POPM [EX]
    JMP :7
;============================
; Checks if we go through all
; ram
; Input: ram index - REX
; 
; If ram index < 100 continue
; checking(:3) if point in
; circle, in other case end
; program
;============================
:7
    PUSHR REX
    PUSH 1
    ADD
    POPR REX
    ;debug PUSHR REX
    ;debug OUT
    PUSHR REX
    PUSH 100
    JA :3
    RET  