CALL :3
:3
    CALL :4 ; input
    CALL :5 ; check that n is not null
    CALL :6 ; check that n is not 1
    CALL :7 ; put 1 to registr with result
    CALL :2 ; find factorial
    CALL :8 ; output

;============================
; Input n
; Output: n - RAX
;============================
:4
    IN
    POPR RAX
    RET
;============================
; Check if n != 0
; Input: n - rax,
; If n == 0 jump to :1
;============================
:5
    PUSH 0
    PUSHR RAX
    JE :1
    RET
;============================
; Check if n != 1
; Input: n - rax,
; If n == 1 jump to :1
;============================
:6
    PUSH 1
    PUSHR RAX
    JE :1
    RET
;============================
; Put 1 to registr with result
; (RCX) registr
;============================
:7
    PUSH 1
    POPR RBX
    PUSHR RBX
    POPR RCX
    RET
;============================
; Output result(RCX)
; End program
;============================
:8
    PUSHR RCX
    OUT
    HLT
;============================
; Output result if n==0 or n==1
; End program
;============================
:1
    PUSH 1
    OUT
    HLT
;============================
; Find factorial by formule
; n! = n * (n - 1)!
; Input: n                 - rax,
;        t (from 2 to n-1) - rbx,
; Output: (n - k)! - rcx
; Call itself many times while
; t < n
;============================
:2  
    PUSHR RBX
    PUSHR RAX
    JBE :3
    PUSHR RBX
    PUSH 1
    ADD
    POPR RBX
    PUSHR RBX
    PUSHR RCX
    MUL
    POPR RCX
    CALL :2
:3  
    RET